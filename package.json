{
  "name": "mjml-mailchimp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "mjml --watch src/* -o html",
    "build": "mjml src/* -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mjml": "^4.13.0",
    "mjml-cli": "^4.13.0",
    "mjml-mailchimp": "^0.0.7"
  }
}
